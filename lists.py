"""lists.py

Assessment: List Skills
"""


def print_indices(items):
    """Print each item in the list, followed by its index."""


def words_in_common(words1, words2):
    """Take in two lists and return a list of items found in both."""


def every_other_item(items):
    """Return every other item in the given list"""


def every_last_char(strings):
    """Return a list containing the last character of each string."""


def get_highest_score(scores):
    """Return the name of the person with the highest score.

    `scores` is a list of tuples that look like `(name, location, score)`.
    """


def smallest_n_nums(nums, n):
    """Return a set of the `n` smallest numbers in the given list of numbers."""
